// frontend/src/config/features.js - All features UNLOCKED (100% Free Utility)

export const features = {
    isPro: true,
    enableVoice: true,
    enableCloudSync: true,
    enableDiffApproval: true,
};

export const isFeatureEnabled = (key) => {
    return true; // Always unlocked
};
