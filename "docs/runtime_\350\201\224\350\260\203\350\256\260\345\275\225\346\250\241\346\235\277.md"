# Runtime 联调记录模板

使用方式：
- 每个 runtime 复制一份本模板。
- 一次联调只记录一个 runtime。
- 不要省略失败原因和证据。

## 基本信息

- Runtime:
- 日期:
- 执行人:
- VS Code bridge 版本:
- Backend 地址:
- 当前 active runtime:

## 环境前提

- VS Code bridge 已连接：`yes / no`
- 手机端已进入同一 session：`yes / no`
- runtime 已启动：`yes / no`
- runtime 健康状态：`ready / degraded / offline`

## 能力验收结果

| 能力 | 结果 | 现象 | 失败原因 | 证据 |
| --- | --- | --- | --- | --- |
| `prompt.submit` | pending |  |  |  |
| `workspace.focus` | pending |  |  |  |
| `context.request/result` | pending |  |  |  |
| `approval.request/response` | pending |  |  |  |
| `kill.request/result` | pending |  |  |  |
| `run_script` | pending |  |  |  |

结果填写规则：
- `full`
- `degraded`
- `unsupported`

## UI 一致性检查

- 能力三态是否正确显示：
- unsupported 按钮是否禁用：
- 最近失败原因是否可见：
- 审计事件是否可回看：

## 结论

- 总体结论：`ready_to_expand / fix_before_expand / blocked`
- 下一步：
- 需要修复的文件：
- 备注：
